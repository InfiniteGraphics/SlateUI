name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test:
    name: Java tests
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        java: [ '21' ]
        task:
          - test
          - :slateui-core:test
          - :slateui-minecraft:compileJava
          - :fabric:build
          - :neoforge:build
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Run tests
        run: .\gradlew.bat ${{ matrix.task }} --stacktrace
