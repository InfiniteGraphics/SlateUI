<template>
  <OverlayRoot>
    <ScrollView style-width="360" style-height="220">
      <Stack direction="column" class="gallery">
        <Stack direction="column" class="panel">
          <Text value="Authoring Gallery" />
          <Text value="{settings.playerName + ' / ' + settings.status}" />
        </Stack>
        <Stack direction="column" class="panel">
          <Input placeholder="Authoring player name" value="{settings.playerName}" onChange="authoring.name" style-width="300" />
          <Button label="Reload Slate" command="slate.reload" />
          <Button label="Close" command="screen.close" />
        </Stack>
        <Stack direction="column" class="panel">
          <Image resource="minecraft:textures/gui/options_background.png" style-width="300" style-height="64" />
        </Stack>
      </Stack>
    </ScrollView>
  </OverlayRoot>
</template>

<style scoped>
  .gallery {
    gap: 12;
  }
  .panel {
    padding: 12;
    backgroundToken: color.panel;
    borderColorToken: color.border;
    borderThickness: 1;
    gap: 8;
  }
</style>
