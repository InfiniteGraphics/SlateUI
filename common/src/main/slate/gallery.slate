<template>
  <OverlayRoot>
    <ScrollView style-width="360" style-height="240">
      <Stack direction="column" class="gallery">
        <Panel title="Authoring Gallery" class="panel">
          <Text value="{settings.playerName + ' / ' + settings.status}" />
          <Toggle label="Enable feature" checked="{settings.enabled}" />
        </Panel>
        <Panel title="Controls" class="panel">
          <Input id="authoringName" placeholder="Authoring player name" value="{settings.playerName}" onChange="authoring.name" style-width="300" />
          <Button label="Reload Slate" command="slate.reload" />
          <Button label="Close" command="screen.close" />
        </Panel>
        <Panel title="List smoke test" class="panel">
          <List style-height="72" itemGap="4">
            <Text value="Settings example" />
            <Text value="Form example" />
            <Text value="Container example" />
          </List>
        </Panel>
        <Panel title="Image" class="panel">
          <Image resource="minecraft:textures/gui/options_background.png" style-width="300" style-height="64" />
        </Panel>
      </Stack>
    </ScrollView>
  </OverlayRoot>
</template>

<style scoped>
  OverlayRoot {
    padding: 12;
  }
  .gallery {
    gap: 12;
  }
  .panel {
    padding: 12;
    backgroundToken: color.panel;
    borderColorToken: color.border;
    borderThickness: 1;
    borderRadius: 8;
    gap: 8;
  }
  .panel:hover {
    background: #111827;
  }
  #authoringName:focus {
    focusBorderColor: #60A5FA;
  }
  Button:hover {
    background: #2563EB;
  }
</style>
