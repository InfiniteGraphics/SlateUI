package top.huliawsl.slateui.api.component;

import top.huliawsl.slateui.api.SlateStyle;

public final class SearchBox extends Input {

    public SearchBox(String initialValue, String changeCommand, SlateStyle style) {
        super("Search", initialValue, changeCommand, style);
    }
}
